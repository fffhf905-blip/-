# -
خاص بمشتركين حساب السقف الثانوي 

🤖 بوت تقارير السقف الثانوي (Telegram Secondary Roof Reports Bot)
🌟 نظرة عامة على المشروع
هذا المشروع يمثل بوت تليجرام آلي مصمم لأتمتة عملية إرسال تقارير قياسات السقف الثانوي (Secondary Ceiling Measurements) بشكل مباشر إلى المستخدمين المعنيين عبر حساباتهم الشخصية في تليجرام.
الهدف الأساسي هو ربط معرّف المستخدم في نظام القياسات الخارجي (Measurement ID) مع معرف الدردشة الخاص به في تليجرام (Telegram Chat ID)، واستخدام هذا الربط لتوجيه التقارير الفورية أو اليومية.
🛠️ التقنيات المستخدمة
| المكون | التقنية / الخدمة | الغرض |
|---|---|---|
| الخلفية والـ Webhook | Python / Flask | لإنشاء نقطة نهاية (Endpoint) لاستقبال تحديثات تليجرام. |
| قاعدة البيانات | Firebase Firestore | لتخزين روابط المستخدمين وحالاتهم (ربط Chat ID بـ Measurement ID). |
| التنفيذ السحابي | Render / خدمة ويب مشابهة | لنشر التطبيق والحصول على عنوان Webhook ثابت. |
| الواجهة الأمامية | Telegram Bot API | للتعامل مع الرسائل والأوامر والمحادثات. |
✨ ميزات البوت والأوامر
تم تصميم البوت لدعم سير عمل التحقق من الهوية وتقديم الدعم الفوري.
| الأمر | الوصف | المنطق الأساسي |
|---|---|---|
| /start | بدء التشغيل والترحيب. | يرسل رسالة ترحيب ثابتة ويوجّه للتحقق. |
| /verify | عملية ربط حساب القياسات بالبوت. | يطلب معرّف القياس (Measurement ID) وينتظر الرد لتخزينه في Firestore. |
| /status | عرض حالة الربط الحالية للمستخدم ونوع التقارير المفعل. | يجلب بيانات المستخدم من Firestore ويعرض الـ Measurement ID وحالة التقرير (فوري/يومي). |
| /admin_panel | أوامر الإدارة ولوحة التحكم. | يتطلب تحقق أمان (Chat ID) قبل عرض قائمة الإدارة. |
| /help | عرض قائمة بجميع الأوامر المتاحة. | يعرض هذه القائمة. |
🔑 الإعداد والنشر (Deployment Setup)
يتطلب تشغيل هذا البوت بنجاح إعداد متغيرات البيئة التالية وربط الـ Webhook.
1. متغيرات البيئة (Environment Variables)
يجب إعداد هذه المتغيرات في منصة النشر (مثل Render) لتأمين الوصول لقاعدة البيانات.
| المتغير | الوصف | القيمة |
|---|---|---|
| TELEGRAM_BOT_TOKEN | الرمز السري لبوت تليجرام. | 8216554417:AAEs3O3y2lYLezGrLliBqXs0zPgpYU9srUg |
| __app_id | معرّف التطبيق الخاص بالمنصة السحابية (Canvas ID). | يُزود تلقائيًا / default-app-id |
| __firebase_config | إعدادات تهيئة Firebase (JSON). | تُزود تلقائيًا |
| __initial_auth_token | رمز المصادقة الأولي لـ Firebase. | يُزود تلقائيًا |
2. إعداد الـ Webhook
بعد نشر التطبيق بنجاح والحصول على عنوان الـ URL العام (مثلاً: https://your-bot-name.onrender.com)، يجب ضبط الـ Webhook عبر Telegram API.
استبدل [عنوان URL الخاص بك] بعنوان الخدمة التي نشرتها:
# استخدم الرمز السري الخاص بالبوت وعنوان الويب الذي حصلت عليه من Render
curl -F "url=[https://your-bot-name.onrender.com/webhook](https://your-bot-name.onrender.com/webhook)" \
"[https://api.telegram.org/bot8216554417:AAEs3O3y2lYLezGrLliBqXs0zPgpYU9srUg/setWebhook](https://api.telegram.org/bot8216554417:AAEs3O3y2lYLezGrLliBqXs0zPgpYU9srUg/setWebhook)"

بمجرد إرسال هذا الطلب، سيتم توجيه جميع رسائل البوت إلى مسار /webhook في تطبيق Flask الخاص بك (app.py).
📂 بنية الكود
يتم تنفيذ المنطق الرئيسي في ملف واحد app.py، والذي يقوم بما يلي:
 * قراءة متغيرات البيئة.
 * تعريف مسار /webhook لاستقبال تحديثات POST.
 * معالجة الأوامر مثل /verify و /status عبر محاكاة وظائف التفاعل مع Firebase Firestore.
